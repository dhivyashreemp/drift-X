{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22",
    "git": "latest"
  },
  "build": {
    "commands": [
      "pip install -r requirements.txt",
      "cd frontend && npm ci && npm run build"
    ]
  }
}
